query getSingleProjectBySlug($slug: String!) {
    project(where: { slug: $slug }) {
        id,
        title,
        slug,
        featured,
        readTime,
        introduction,
        content {
            markdown
            html
            raw
        },
        thumbnail {
            id,
            url
        },
        tags {
            id,
            value
            colorScheme
        },
        createdAt,
    }
}
