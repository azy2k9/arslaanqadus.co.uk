query getFeaturedBlogs {
    blogs(where: { featured: true }) {
        id
        title
        slug
        featured
        thumbnail {
            id
            url
        }
        introduction
        createdAt
        readTime
        tags {
            id
            value
            colorScheme
        }
    }
}
